apiVersion: apps/v1
kind: Deployment
metadata:
  name: qpid-dispatcher-deployment-a
  labels:
    app: qpid-dispatcher-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qpid-dispatcher-a
  template:
    metadata:
      labels:
        app: qpid-dispatcher-a
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: yolo
                    operator: In
                    values:
                      - yolo
      containers:
        - name: qpid-dispatcher-a
          image: quay.io/interconnectedcloud/qdrouterd:1.14.0
          resources:
            requests:
              cpu: "50m"
            limits:
              cpu: "50m"
          volumeMounts:
            - name: qpidrouter-config-a
              mountPath: /etc/qpid-dispatch/
      volumes:
          - name: qpidrouter-config-a
            configMap:
              name: qpidrouter-config-a
              items:
                - key: qdrouterd.conf
                  path: qdrouterd.conf
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qpid-dispatcher-deployment-b
  labels:
    app: qpid-dispatcher-b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qpid-dispatcher-b
  template:
    metadata:
      labels:
        app: qpid-dispatcher-b
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: yolo
                    operator: In
                    values:
                      - yolo
      containers:
        - name: qpid-dispatcher-b
          image: quay.io/interconnectedcloud/qdrouterd:1.14.0
          resources:
            requests:
              cpu: "50m"
            limits:
              cpu: "50m"
          volumeMounts:
            - name: qpidrouter-config-b
              mountPath: /etc/qpid-dispatch/
      volumes:
        - name: qpidrouter-config-b
          configMap:
            name: qpidrouter-config-b
            items:
              - key: qdrouterd.conf
                path: qdrouterd.conf
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qpid-dispatcher-deployment-c
  labels:
    app: qpid-dispatcher-c
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qpid-dispatcher-c
  template:
    metadata:
      labels:
        app: qpid-dispatcher-c
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: yolo
                    operator: In
                    values:
                      - yolo
      containers:
        - name: qpid-dispatcher-c
          image: quay.io/interconnectedcloud/qdrouterd:1.14.0
          resources:
            requests:
              cpu: "50m"
            limits:
              cpu: "50m"
          volumeMounts:
            - name: qpidrouter-config-c
              mountPath: /etc/qpid-dispatch/
      volumes:
        - name: qpidrouter-config-c
          configMap:
            name: qpidrouter-config-c
            items:
              - key: qdrouterd.conf
                path: qdrouterd.conf
